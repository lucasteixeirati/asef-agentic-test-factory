# SMK-003 — Normalização de espaços

`normalize_whitespace(value)` deve remover espaços nas extremidades e substituir qualquer sequência interna de whitespace por um único espaço. Entrada vazia ou contendo apenas whitespace deve retornar string vazia.
