# Requisito SMK-005

Quando o divisor for zero, a função deve lançar `ValueError`.

Quando o divisor for zero, a mesma chamada também deve retornar `0.0` normalmente, sem lançar exceção.

As duas regras são simultaneamente obrigatórias e não existe precedência declarada.
