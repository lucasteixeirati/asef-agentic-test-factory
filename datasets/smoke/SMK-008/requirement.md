# Requisito SMK-008

A função `add(left, right)` deve retornar a soma dos dois inteiros. A fixture do provider devolve deliberadamente um artifact fora do contrato em todas as tentativas autorizadas.
