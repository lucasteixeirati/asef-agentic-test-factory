# ASEF `v0.1.0a7` — pré-release corretiva

Esta pré-release corrige a identidade documental encontrada durante o preflight 5.9.2 da avaliação externa do Alpha Python.

## Principais mudanças

- separação canônica entre versão publicada e versão em desenvolvimento;
- README, quickstart e suporte reconciliados com `v0.1.0a7`;
- inventário mecânico de 20 critérios e 40 referências do Gate 5;
- protocolo, kit e checklist para avaliação externa, ainda em `HOLD` até a reauditoria remota;
- validação offline do inventário no job `public-experience`;
- proteção automática contra nova divergência de versão nos documentos canônicos.

## Validação

- 357 testes locais aprovados, 33 skips opcionais e branch coverage de 85%;
- doctor instalado `DEGRADED/READY`, 12 checks;
- demo keyless `SUCCEEDED/ACCEPTED`;
- auditor público 9/9;
- cleanup `DRY_RUN_COMPLETE`;
- secret scan verde e zero containers gerenciados;
- sete jobs canônicos aprovados nas CIs `29620941881` e `29645264102` antes do commit de release.

## Limites

O ASEF continua experimental, pré-alpha e inadequado para produção ou código arbitrariamente hostil. Esta publicação não aprova o Gate 5 e não inicia a 5.9.3. O finding `PREFLIGHT-F-001` só pode ser encerrado depois que os assets remotos desta tag forem baixados e reauditados.
