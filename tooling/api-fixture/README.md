# Backend API fixture tooling

O driver canônico é empacotado em `src/asef/fixtures/api_fixture_driver.py`. `DockerApiFixtureExecutor.stage` materializa uma cópia e o plano em workspace efêmero antes da conformance. Não mantenha uma segunda implementação neste diretório.

