# ASEF Web UI fixture

Static, fictitious and resettable application for Stage 6.4 conformance. It has no
external dependencies, persistence, credentials or network calls. The fixture is
is executed only by the bounded Stage 6.4.3 driver with the server and Chromium in
the same networkless container. It is not evidence of support for external sites.
